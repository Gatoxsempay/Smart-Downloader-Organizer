<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="OrganizadorDescargas.Pages.DuplicatesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="Transparent"
    NavigationCacheMode="Required">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="30,30,30,16">
            <StackPanel>
                <TextBlock Text="Buscador de duplicados" FontSize="24" FontWeight="Bold" />
                <TextBlock Text="Detecta archivos idénticos por tamaño + hash MD5 para liberar espacio"
                           FontSize="13" Margin="0,2,0,0"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
        </Grid>

        <!-- Barra de configuración -->
        <Border Grid.Row="1" Margin="30,0,30,12"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="12" Padding="16,12">
            <Grid ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Analiza automáticamente Descargas, Escritorio, Documentos, Imágenes, Videos y Música en busca de archivos idénticos."
                           VerticalAlignment="Center" FontSize="12" TextWrapping="Wrap"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                <Button x:Name="ScanBtn" Grid.Column="1"
                        Content="Volver a escanear"
                        Background="#2563EB" Foreground="White" FontWeight="SemiBold"
                        Padding="16,8"
                        Click="ScanBtn_Click" />
            </Grid>
        </Border>

        <!-- Barra de progreso / estado -->
        <Border x:Name="StatusBar" Grid.Row="2" Visibility="Collapsed"
                Margin="30,0,30,12"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="12" Padding="16,10">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <ProgressRing x:Name="ScanRing" Width="20" Height="20" IsActive="True"
                              VerticalAlignment="Center" />
                <TextBlock x:Name="StatusText" FontSize="12" VerticalAlignment="Center"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
        </Border>

        <!-- Resultados -->
        <ScrollViewer Grid.Row="3" Margin="30,0,30,0">
            <StackPanel x:Name="ResultsList" Spacing="10" />
        </ScrollViewer>

        <!-- Barra de acciones -->
        <Border x:Name="ActionBar" Grid.Row="4" Visibility="Collapsed"
                Margin="30,12,30,30"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="12" Padding="16,12">
            <Grid>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button x:Name="SelectAllBtn" Content="Seleccionar duplicados (mantener primero)"
                            Background="#374151" Foreground="White"
                            Click="SelectAllBtn_Click" />
                    <Button x:Name="DeselectBtn" Content="Deseleccionar todo"
                            Background="#374151" Foreground="White"
                            Click="DeselectBtn_Click" />
                </StackPanel>
                <Button x:Name="DeleteBtn"
                        Content="Eliminar seleccionados"
                        HorizontalAlignment="Right"
                        Background="#DC2626" Foreground="White" FontWeight="SemiBold"
                        Padding="16,8"
                        Click="DeleteBtn_Click" />
            </Grid>
        </Border>
    </Grid>
</Page>
